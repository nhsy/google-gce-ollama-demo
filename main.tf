# Resources are organized across purpose-specific files:
#   apis.tf        — API enablement
#   networking.tf  — VPC, subnet, firewall
#   storage.tf     — GCS model cache bucket
#   iam.tf         — service account and IAM bindings
#   compute.tf     — GCE instance
